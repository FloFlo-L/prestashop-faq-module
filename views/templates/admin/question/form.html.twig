{% extends '@PrestaShop/Admin/layout.html.twig' %}

{% block content %}
{{ form_start(faqForm, {action: formAction}) }}
<div class="row justify-content-center">
  <div class="col-xl-8">
    <div class="card">
      <div class="card-header">
        <h3 class="card-header-title">
          {{ layoutTitle }}{% if entityId is defined %} <small class="text-muted">#{{ entityId }}</small>{% endif %}
        </h3>
      </div>
      <div class="card-body">
        {{ form_row(faqForm.id_faq_category) }}
        {{ form_row(faqForm.question) }}
        {{ form_row(faqForm.answer) }}
        {{ form_row(faqForm.active) }}
      </div>
      <div class="card-footer">
        <div class="d-flex justify-content-between">
          <a href="{{ path('faq_question_index') }}" class="btn btn-secondary">
            {{ 'Cancel'|trans({}, 'Admin.Actions') }}
          </a>
          <button type="submit" class="btn btn-primary">
            {{ 'Save'|trans({}, 'Admin.Actions') }}
          </button>
        </div>
      </div>
    </div>
  </div>
</div>
{{ form_end(faqForm) }}
{% endblock %}

{% block javascripts %}
  {{ parent() }}
  <script src="{{ asset('../modules/faq/views/js/admin/faq_question_form.js') }}"></script>
{% endblock %}
